const execute = (bot,msg,args) => {
    return msg.channel.send('Meu criador e melhor Kennen da roça onde ele mora')
    };

module.exports = {
    name: "radio",
    help:"Sem descrições, é o Radio",
    execute,
}